# P0.7: Truth in Advertising (README Update)

## Goal
Update `README.md` so that the architectural claims perfectly match the hardened implementation.

## Requirements
1. **Remove Prototype Language**: Remove claims of "mathematically preventing" exfiltration; replace with standard security engineering terms (e.g., "reduces attack surface via network-isolated, read-only volume mounts and strict container identity binding").
2. **Update the Validator Description**: Explicitly state that the `validator_node` uses an `ast.NodeVisitor` for intra-procedural taint-tracking (verifying `source -> flow -> sink`), and that regex heuristics have been entirely removed.
3. **Update the Sandbox Lifecycle**: Mention that CipherLoop validates the target mount hash of the Docker container before execution to prevent cross-contamination.
4. **Update the Telemetry section**: State that the `TrajectoryRecorder` calculates exact compression ratios (`raw_char_count` vs `compressed_char_count`) and candidate-to-verified ratios for empirical evaluation.
5. **Formatting**: Maintain the existing hero image, Mermaid graph, and markdown structure.

## Definition of Done
The README reads like a senior security engineering portfolio piece, claiming exactly what the system does today, with no vaporware.
